# llvm1: LLVM llrl API

LLVM-C API binding for llrl programming language.

Unlike `llvm0`, `llvm1` does both binding C API functions and providing high-level abstract interfaces. Nevertheless, many functions are simply re-exports of the LLVM-C API with llrl's naming conventions.
